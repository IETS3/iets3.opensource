package test.org.iets3.analysis.base.async;

/*Generated by MPS */

import jetbrains.mps.MPSLaunch;
import jetbrains.mps.lang.test.runtime.BaseTransformationTest;
import org.junit.jupiter.api.extension.RegisterExtension;
import jetbrains.mps.lang.test.runtime.TestParametersCacheExtension;
import jetbrains.mps.lang.test.runtime.TestParametersCacheBuilder;
import org.junit.jupiter.api.Test;
import jetbrains.mps.lang.test.runtime.BaseTestBody;
import jetbrains.mps.lang.test.runtime.TransformationTest;
import jetbrains.mps.baseLanguage.closures.runtime.Wrappers;
import org.jetbrains.mps.openapi.model.SModel;
import jetbrains.mps.smodel.tempmodel.TemporaryModels;
import jetbrains.mps.smodel.tempmodel.TempModuleOptions;
import org.junit.Assert;
import java.util.Random;
import org.iets3.analysis.base.plugin.ExecOnPooledThreadInWriteAccess;
import org.iets3.analysis.base.plugin.ITask;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import org.iets3.core.base.behavior.IResult;
import java.util.stream.Stream;
import java.util.stream.Collectors;
import java.util.Collections;
import java.util.concurrent.TimeUnit;
import java.util.concurrent.ExecutionException;
import java.util.concurrent.TimeoutException;
import org.iets3.analysis.base.plugin.AsyncSolverTaskExecutor;
import org.jetbrains.mps.openapi.model.SNode;
import java.util.Collection;
import java.time.Duration;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.smodel.builder.SNodeBuilder;
import org.jetbrains.mps.openapi.language.SConcept;
import jetbrains.mps.smodel.adapter.structure.MetaAdapterFactory;
import org.jetbrains.mps.openapi.language.SProperty;

@MPSLaunch
public class TestAsyncFromSingleThread_Test extends BaseTransformationTest {
  @RegisterExtension
  private static final TestParametersCacheExtension ourParametersCacheExtension = new TestParametersCacheExtension(new TestParametersCacheBuilder(TestAsyncFromSingleThread_Test.class).projectPath(null).modelRef("r:323df21b-bcb0-4525-967d-a41bb2c3fcb8(test.org.iets3.analysis.base.async@tests)").reopenProject(null).build());

  public TestAsyncFromSingleThread_Test() {
    super(ourParametersCacheExtension.getParametersCache());
  }

  @Test
  public void test_singleThread() throws Throwable {
    new TestBody(this).test_singleThread();
  }

  /*package*/ static class TestBody extends BaseTestBody {

    /*package*/ TestBody(TransformationTest owner) {
      super(owner);
    }

    @Override
    protected void initTestNodes() {
      prepareTestNodes();
    }

    public void test_singleThread() throws Exception {
      initTestNodes();
      final Wrappers._T<SModel> modelTmp = new Wrappers._T<SModel>();

      try {
        myProject.getRepository().getModelAccess().runWriteAction(() -> modelTmp.value = TemporaryModels.getInstance().createEditable(false, TempModuleOptions.forDefaultModule()));

        Assert.assertNotNull("Could not create Temp-Model", modelTmp.value);
        // Check that a bunch of solvables can be scheduled successfully 
        final Random random = new Random(System.nanoTime());
        int no = 9;
        final ExecOnPooledThreadInWriteAccess<ITask> execEnv = new ExecOnPooledThreadInWriteAccess<>(myProject.getRepository());

        List<CompletableFuture<List<IResult>>> futures = Stream.generate(() -> TestBody.this.schedule(random, execEnv, modelTmp.value)).limit(no).collect(Collectors.<CompletableFuture<List<IResult>>>toList());

        Collections.shuffle(futures);

        final Wrappers._int i = new Wrappers._int(0);
        futures.forEach((CompletableFuture<List<IResult>> future) -> {
          try {
            List<IResult> result = future.get(5, TimeUnit.SECONDS);
            i.value++;
            TestBody.this.check(result);
          } catch (InterruptedException e) {
            assert false;
          } catch (ExecutionException e) {
            assert false;
          } catch (TimeoutException e) {
            assert false;
          }
        });
        // All Tasks should return
        Assert.assertEquals(Integer.valueOf(no), Integer.valueOf(i.value));
      } finally {
        if (modelTmp.value != null) {
          myProject.getRepository().getModelAccess().runWriteAction(() -> TemporaryModels.getInstance().dispose(modelTmp.value));

        }
      }
    }

    public CompletableFuture<List<IResult>> schedule(Random r, AsyncSolverTaskExecutor.IExecEnv<ITask> execEnv, final SModel model) {

      final SNode solvable = createDummyISolvable_n8wjnp_a0b0h6(TestUtil.sleepTime(r, 1000));


      myProject.getRepository().getModelAccess().runWriteAction(() -> model.addRootNode(solvable));


      AsyncSolverTaskExecutor.IExecEnv<Collection<IResult>> execEnv2 = AsyncSolverTaskExecutor.<Collection<IResult>>makeReadAccessEnv(myProject.getRepository());

      return AsyncSolverTaskExecutor.submitISolvable(solvable, (SNode task) -> {
      }, Duration.ofMillis(100), true, execEnv, execEnv2);
    }
    public void check(List<IResult> results) {
      for (IResult result : ListSequence.fromList(results)) {
        String errMsg = (String) result.getErrorMessage();
        Assert.assertEquals(errMsg, IResult.MessageType.OK, result.messageType());
      }
    }
    private static SNode createDummyISolvable_n8wjnp_a0b0h6(int p0) {
      SNodeBuilder n0 = new SNodeBuilder().init(CONCEPTS.DummyISolvable$_U);
      n0.setProperty(PROPS.sleepTime$rMiT, "" + (p0));
      return n0.getResult();
    }
  }

  private static final class CONCEPTS {
    /*package*/ static final SConcept DummyISolvable$_U = MetaAdapterFactory.getConcept(0x5a0fae258093498fL, 0x81fe3b264864819aL, 0x782a6ab7cbd22cbbL, "test.org.iets3.analysis.base.solvable.structure.DummyISolvable");
  }

  private static final class PROPS {
    /*package*/ static final SProperty sleepTime$rMiT = MetaAdapterFactory.getProperty(0x5a0fae258093498fL, 0x81fe3b264864819aL, 0x782a6ab7cbd22cbbL, 0x782a6ab7cbd239d2L, "sleepTime");
  }
}
