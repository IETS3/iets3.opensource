package test.ts.expr.os.phyunits;

/*Generated by MPS */

import jetbrains.mps.MPSLaunch;
import jetbrains.mps.lang.test.runtime.BaseTransformationTest;
import org.junit.jupiter.api.extension.RegisterExtension;
import jetbrains.mps.lang.test.runtime.TestParametersCacheExtension;
import jetbrains.mps.lang.test.runtime.TestParametersCacheBuilder;
import org.junit.jupiter.api.Test;
import jetbrains.mps.lang.test.runtime.BaseTestBody;
import jetbrains.mps.lang.test.runtime.TransformationTest;
import org.junit.Assert;
import org.iets3.core.expr.typetags.physunits.behavior.UnitNameReplacementHelper;

@MPSLaunch
public class UnitNameReplacement_Test extends BaseTransformationTest {
  @RegisterExtension
  private static final TestParametersCacheExtension ourParametersCacheExtension = new TestParametersCacheExtension(new TestParametersCacheBuilder(UnitNameReplacement_Test.class).projectPath(null).modelRef("r:f845bafd-372b-4de8-9dc7-8243d82ec407(test.ts.expr.os.phyunits@tests)").reopenProject(null).build());

  public UnitNameReplacement_Test() {
    super(ourParametersCacheExtension.getParametersCache());
  }

  @Test
  public void test_replaceSpecialCharacters() throws Throwable {
    new TestBody(this).test_replaceSpecialCharacters();
  }

  /*package*/ static class TestBody extends BaseTestBody {

    /*package*/ TestBody(TransformationTest owner) {
      super(owner);
    }

    @Override
    protected void initTestNodes() {
      prepareTestNodes();
    }

    public void test_replaceSpecialCharacters() throws Exception {
      initTestNodes();
      runWithinCommand(() -> {
        Assert.assertEquals("m/s^4", UnitNameReplacementHelper.replaceUnitName("m÷s⁴"));

        Assert.assertEquals("F", UnitNameReplacementHelper.replaceUnitName("Φ"));

        Assert.assertEquals("e", UnitNameReplacementHelper.replaceUnitName("ε"));

        Assert.assertEquals("r_a", UnitNameReplacementHelper.replaceUnitName("ρₐ"));

        Assert.assertEquals("E_e", UnitNameReplacementHelper.replaceUnitName("Eₑ"));

        Assert.assertEquals("l_m", UnitNameReplacementHelper.replaceUnitName("λₘ"));

        Assert.assertEquals("m", UnitNameReplacementHelper.replaceUnitName("ṁ"));

        Assert.assertEquals("h", UnitNameReplacementHelper.replaceUnitName("h⃗"));

        Assert.assertEquals("Ns", UnitNameReplacementHelper.replaceUnitName("N⋅s"));

        Assert.assertEquals("R", UnitNameReplacementHelper.replaceUnitName("ℛ"));

        Assert.assertEquals("F", UnitNameReplacementHelper.replaceUnitName("Ƒ"));

        Assert.assertEquals("m^1", UnitNameReplacementHelper.replaceUnitName("m¹"));

        Assert.assertEquals("m^-1", UnitNameReplacementHelper.replaceUnitName("m⁻¹"));

        // two letters that look like greek letters but are not
        Assert.assertEquals("H", UnitNameReplacementHelper.replaceUnitName("H"));
        Assert.assertEquals("X", UnitNameReplacementHelper.replaceUnitName("X"));
      });
    }

  }
}
