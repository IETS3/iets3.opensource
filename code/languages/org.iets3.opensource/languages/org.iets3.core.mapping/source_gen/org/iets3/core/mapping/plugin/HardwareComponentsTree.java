package org.iets3.core.mapping.plugin;

/*Generated by MPS */

import org.jetbrains.mps.openapi.model.SNode;

public class HardwareComponentsTree extends Tree {

  private HardwareComponentsTree() {
    // should never be called
  }

  protected HardwareComponentsTree(SNode rootInstance) {
    initTree(rootInstance);
  }

  private void initTree(SNode rootInstance) {
    this.treeRoot = HardwareTreeRoot.fromMapping(rootInstance);
  }
}
