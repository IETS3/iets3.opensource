package org.iets3.variability.base.plugin;

/*Generated by MPS */

import jetbrains.mps.baseLanguage.closures.runtime._FunctionTypes;

/**
 * Abstract base class for extension implementations in tests.
 * 
 * Using a typical pattern, the priority of the extension can be changed by tests in order to activate it.
 */
public abstract class TestableExtensionWithPriority extends AbstractExtensionWithPriority implements IExtensionWithPriority {
  protected static final int PRIO_INACTIVE = DEFAULT_PRIORITY - 10000;
  protected static final int PRIO_ACTIVE = DEFAULT_PRIORITY + 10000;

  private static int myPriority = PRIO_INACTIVE;

  public static void doTest(_FunctionTypes._void_P0_E0 action) {
    switchOn();
    action.invoke();
    switchOff();
  }

  public static void switchOn() {
    myPriority = PRIO_ACTIVE;
  }

  public static void switchOff() {
    myPriority = PRIO_INACTIVE;
  }

  @Override
  public int priority() {
    return myPriority;
  }
}
