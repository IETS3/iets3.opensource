package org.iets3.variability.base.plugin;

/*Generated by MPS */

import java.util.function.Supplier;
import jetbrains.mps.internal.collections.runtime.Sequence;

public abstract class AbstractExtensionProvider {

  protected static <R extends IExtensionWithPriority> R instanceByPriority(Iterable<R> extensions, Supplier<R> defaultSupplier) {
    R ep = Sequence.fromIterable(extensions).sort((it) -> it.priority(), false).first();

    // null case is fallback, should not happen
    return (ep == null ? defaultSupplier.get() : ep);
  }
}
