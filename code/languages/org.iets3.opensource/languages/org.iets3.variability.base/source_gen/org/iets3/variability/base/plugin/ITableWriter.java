package org.iets3.variability.base.plugin;

/*Generated by MPS */


/**
 * This interface is used by the variant analysis to write the results into a table.
 * 
 * The interface could be implemented to export an Excel file, a csv file,
 * but also to store the data in memory for tests.
 */
public interface ITableWriter {
  void setValue(int row, int col, String content);
  void setValue(int row, int col, int content);
  void setStyleVertical(int row, int col);

  void addHyperlink(int row, int col, String url);
  void addHyperlinkVertical(int row, int col, String url);

  void autoSizeColumn(int col);
}
