package org.iets3.variability.artifacts.base.plugin;

/*Generated by MPS */

import java.util.Collection;
import org.jetbrains.mps.openapi.model.SNode;
import java.util.Collections;

/**
 * An IFilterInstantiator can filter an artifact model from 150% to 100% (i.e. bind variation points and
 * filter the model).
 * 
 * Note that some IFilterInstantiator implementations might have restrictions. Typical functionality
 * which is not supported by the "base" IFilterInstantiator is this:
 * 
 * - filtering with instantiation mechanism
 * - taking into account dependent sources
 * - presence prerequisites
 */
public interface IFilterInstantiator {

  /**
   * Do the actual filtering and instantiation (if supported). 
   * The artifact nodes to be filtered, the variant configuration and other options are provided 
   * by a FilterParams instance, which can be constructed via a builder pattern.
   * 
   * @param filterParams the parameters for this filter action
   * @return detached nodes from input artifact nodes
   */
  default Collection<SNode> filterInPlace(FilterParams filterParams) {
    return Collections.<SNode>emptySet();
  }
}
