package org.iets3.variability.artifacts.base.behavior;

/*Generated by MPS */

import java.util.Map;
import org.jetbrains.mps.openapi.model.SNode;
import java.util.HashMap;

public class StateStoreProviderImpl implements StateStoreProvider {
  private Map<SNode, StateStore> vpToState = new HashMap<SNode, StateStore>();

  @Override
  public StateStore get(SNode ivp) {
    StateStore stateStore = this.vpToState.get(ivp);
    if (stateStore == null) {
      stateStore = new StateStore();
      this.vpToState.put(ivp, stateStore);
    }
    return stateStore;
  }
}
