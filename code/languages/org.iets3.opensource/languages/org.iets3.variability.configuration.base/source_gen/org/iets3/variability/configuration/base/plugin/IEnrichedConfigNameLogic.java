package org.iets3.variability.configuration.base.plugin;

/*Generated by MPS */

import org.jetbrains.mps.openapi.model.SNode;

/**
 * Extension API definition for EP "enrichedConfigNameLogicExtPoint"
 */
public interface IEnrichedConfigNameLogic {
  int DEFAULT_PRIORITY = 0;

  /**
   * Checks if the enriched name is actually different from the configuration's manual name.
   * It is recommended to avoid string comparisons, if possible.
   */
  boolean hasActualEnrichedName(SNode config);

  /**
   * Returns enriched name for this configuration. Default: The manually defined name.
   */
  String getEnrichedName(SNode config);

  enum DisplayStrategy {
    DO_NOT_SHOW(),
    SHOW_IN_EDITOR(),
    SHOW_IN_INSPECTOR()
  }

  /**
   * Selects a strategy whether and where the enriched names should be shown to the user.
   * Might be specific to the feature model of the configuration.
   */
  DisplayStrategy getDisplayStrategy(SNode fm);

  /**
   * If true, the enriched names are used for checking the uniqueness of configurations.
   * Otherwise, the manually defined names (i.e., values of property FeatureModelConfiguration.name) are used.
   */
  boolean useEnrichedNameForUniquenessCheck();

  /**
   * The extension implementation with the highest priority wins.
   */
  int priority();
}
