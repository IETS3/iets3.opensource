package org.iets3.analysis.base.plugin;

/*Generated by MPS */

import javax.swing.Icon;
import java.util.Arrays;

public enum ISolvableTaskStatus {
  Timeout(IconContainer.ICON_a0, 6),
  Failure(IconContainer.ICON_a1, 5),
  Running(IconContainer.ICON_a2, 2),
  Done(IconContainer.ICON_a3, 4),
  Rejected(IconContainer.ICON_a4, 4),
  Queued(IconContainer.ICON_a5_0, 3);


  private Icon icon;
  private int sortingPriority;

  private ISolvableTaskStatus(Icon icon, int prio) {
    this.icon = icon;
    this.sortingPriority = prio;
  }
  public Icon icon() {
    return icon;
  }

  public int sortingPriority() {
    return this.sortingPriority;
  }

  public boolean isStopped() {
    return Arrays.asList(ISolvableTaskStatus.Rejected, ISolvableTaskStatus.Timeout, ISolvableTaskStatus.Failure, ISolvableTaskStatus.Done).contains(this);
  }
}
