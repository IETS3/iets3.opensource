package org.iets3.analysis.base.plugin;

/*Generated by MPS */

import org.iets3.core.base.behavior.IResult;
import org.jetbrains.mps.openapi.model.SNode;
import org.apache.commons.lang3.builder.HashCodeBuilder;
import org.apache.commons.lang3.builder.EqualsBuilder;
import com.google.common.base.MoreObjects;

/**
 * Encapsulates all information for UI updates.
 */
public class MessageUI {
  public final IResult.MessageType type;
  public final SNode node;
  public final String message;

  public MessageUI(IResult.MessageType type, SNode node, String message) {
    this.type = type;
    this.node = node;
    this.message = message;
  }


  @Override
  public int hashCode() {
    return new HashCodeBuilder(17, 37).append(type).append(node).append(message).toHashCode();
  }


  @Override
  public boolean equals(Object obj) {
    if (obj == this) {
      return true;
    }
    if (!(obj instanceof MessageUI)) {
      return false;
    }
    MessageUI other = ((MessageUI) obj);

    return new EqualsBuilder().append(this.type, other.type).append(this.node, other.node).append(this.message, other.message).build();
  }

  @Override
  public String toString() {
    return MoreObjects.toStringHelper(this).add("type", type).add("node", node).add("message", message).toString();
  }
}
