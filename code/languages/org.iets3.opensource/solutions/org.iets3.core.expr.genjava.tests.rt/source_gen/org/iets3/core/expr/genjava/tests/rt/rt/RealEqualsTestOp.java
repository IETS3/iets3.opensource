package org.iets3.core.expr.genjava.tests.rt.rt;

/*Generated by MPS */

import java.math.BigDecimal;
import java.math.RoundingMode;

public class RealEqualsTestOp {

  public static boolean matches(Object act, Object exp, int decimals) {
    if (act == null && exp == null) {
      return true;
    } else if (act == null && exp != null) {
      return false;
    } else if (act != null && exp == null) {
      return false;
    } else {
      if (act instanceof BigDecimal && exp instanceof BigDecimal) {
        BigDecimal bda = ((BigDecimal) act);
        BigDecimal bde = ((BigDecimal) exp);
        bda = bda.setScale(decimals, RoundingMode.DOWN);
        bde = bde.setScale(decimals, RoundingMode.DOWN);
        return bda.compareTo(bde) == 0;
      }
      return act.equals(exp);
    }
  }
}
