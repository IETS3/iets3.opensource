package org.iets3.core.expr.datetime.runtime;

/*Generated by MPS */

import java.time.LocalDate;

public class FromUptoHelper {

  public static AbstractDateRangeValue from(AbstractDateRangeValue range, LocalDate newStartDate) {
    LocalDate endDate = range.end();
    if (newStartDate.isAfter(endDate)) {
      return new EmptyDateRangeValue();
    }
    if (newStartDate.isBefore(range.begin())) {
      return range;
    }
    return new ArbitraryDateRangeValue(newStartDate, endDate);
  }

  public static AbstractDateRangeValue upto(AbstractDateRangeValue range, LocalDate newEndDate) {
    LocalDate beginDate = range.begin();
    if (newEndDate.isBefore(beginDate)) {
      return new EmptyDateRangeValue();
    }
    if (newEndDate.isAfter(range.end())) {
      return range;
    }
    return new ArbitraryDateRangeValue(beginDate, newEndDate);
  }


}
