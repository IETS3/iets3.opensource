package org.iets3.core.expr.datetime.runtime;

/*Generated by MPS */

import java.time.LocalDate;
import java.util.Iterator;

public class EarlyLateHelper {

  public static LocalDate earliest(Iterable dates) {
    Iterator it = dates.iterator();
    LocalDate res = LocalDate.MAX;
    while (it.hasNext()) {
      LocalDate d = (LocalDate) it.next();
      if (d.isBefore(res)) {
        res = d;
      }
    }
    return res;
  }

  public static LocalDate latest(Iterable dates) {
    Iterator it = dates.iterator();
    LocalDate res = LocalDate.MIN;
    while (it.hasNext()) {
      LocalDate d = (LocalDate) it.next();
      if (d.isAfter(res)) {
        res = d;
      }
    }
    return res;
  }



}
