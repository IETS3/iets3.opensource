package org.iets3.core.expr.plugin.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import jetbrains.mps.workbench.action.ActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import java.util.Set;
import jetbrains.mps.internal.collections.runtime.SetSequence;
import java.util.HashSet;
import jetbrains.mps.ide.editor.MPSEditorDataKeys;
import java.util.Objects;
import org.iets3.core.expr.base.behavior.IDefaultCoverageAnalyzer;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.nodeEditor.EditorComponent;

public class ShowCoverage_Action extends BaseAction {
  private static final Icon ICON = IconContainer.ICON3;

  public ShowCoverage_Action() {
    super("Show Coverage", "", ICON);
    this.setIsAlwaysVisible(false);
    this.setActionAccess(ActionAccess.UNDO_PROJECT);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  public boolean isApplicable(AnActionEvent event, final Map<String, Object> _params) {
    final String cvg = "org.iets3.core.expr.base.editor.coverage.showCoverage";
    Set<String> hints = SetSequence.fromSetAndArray(new HashSet<String>(), event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getUpdater().getInitialEditorHints());
    if (SetSequence.fromSet(hints).any((it) -> Objects.equals(it, cvg))) {
      return true;
    }
    return IDefaultCoverageAnalyzer.hasData();
  }
  @Override
  public void doUpdate(@NotNull AnActionEvent event, final Map<String, Object> _params) {
    this.setEnabledState(event.getPresentation(), this.isApplicable(event, _params));
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      EditorComponent editorComponent = event.getData(MPSEditorDataKeys.EDITOR_COMPONENT);
      if (editorComponent != null && editorComponent.isInvalid()) {
        editorComponent = null;
      }
      if (editorComponent == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    final String cvg = "org.iets3.core.expr.base.editor.coverage.showCoverage";
    Set<String> hints = SetSequence.fromSetAndArray(new HashSet<String>(), event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getUpdater().getInitialEditorHints());
    if (SetSequence.fromSet(hints).any((it) -> Objects.equals(it, cvg))) {
      SetSequence.fromSet(hints).removeElement(cvg);
    } else {
      SetSequence.fromSet(hints).addElement("org.iets3.core.expr.base.editor.coverage.showCoverage");
      SetSequence.fromSet(hints).addElement("de.slisson.mps.conditionalEditor.hints.editor.conditionalEditorHints.conditionalEditor");
    }
    String[] hq = SetSequence.fromSet(hints).toGenericArray(String.class);
    event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getUpdater().setInitialEditorHints(hq);
    event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getUpdater().update();
  }
}
